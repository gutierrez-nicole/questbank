@extends('layouts.app', ['title' => $subject->exists ? 'Edit Subject' : 'Add Subject'])
@section('content')
<form method="POST" action="{{ $subject->exists ? route('subjects.update',$subject) : route('subjects.store') }}" class="card border-0 shadow-sm">@csrf @if($subject->exists) @method('PUT') @endif
<div class="card-body row g-3"><div class="col-md-4"><label class="form-label">Code</label><input class="form-control" name="code" value="{{ old('code',$subject->code) }}" required></div><div class="col-md-8"><label class="form-label">Subject Name</label><input class="form-control" name="name" value="{{ old('name',$subject->name) }}" required></div><div class="col-md-4"><label class="form-label">Program</label><input class="form-control" name="program" value="Civil Engineering" readonly></div><div class="col-md-8"><label class="form-label">Assign Instructor</label><select class="form-select" name="instructor_id"><option value="">Unassigned</option>@foreach($instructors as $instructor)<option value="{{ $instructor->id }}" @selected(old('instructor_id',$subject->instructor_id)==$instructor->id)>{{ $instructor->full_name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Units</label><input class="form-control" type="number" name="units" value="{{ old('units',$subject->units ?: 3) }}" min="1" max="6" required></div><div class="col-12"><label class="form-label">Description</label><textarea class="form-control" name="description">{{ old('description',$subject->description) }}</textarea></div></div><div class="card-footer bg-white"><button class="btn btn-success">Save Subject</button> <a class="btn btn-outline-secondary" href="{{ route('subjects.index') }}">Cancel</a></div></form>
@endsection
